const ino = ['D', 'B', 'E', 'A', 'F', 'C', 'G'];
const post = ['D', 'E', 'B', 'F', 'G', 'C', 'A'];

class Node {
    value = null;
    left = null;
    right = null;

    constructor(value) {
        this.value = value;
    }
}

function preInToPost(pre, ino) {

}
